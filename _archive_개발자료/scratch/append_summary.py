with open('summary.md', 'a', encoding='utf-8') as f:
    f.write('\n\n## 10. OCR 인식률 극대화 및 급여 명세서 자동 분할 (최신 업데이트)\n')
    f.write('### 1) OCR 전처리(Preprocessing) 강화\n')
    f.write('- 기존 `rapidocr_onnxruntime` 엔진의 장점(빠른 속도, 가벼운 의존성)을 그대로 유지하면서 인식률을 극한으로 끌어올리기 위해 **OpenCV 이미지 전처리 파이프라인**을 `ocr_engine.py`에 새롭게 도입했습니다.\n')
    f.write('- **구현된 파이프라인**: `Grayscale 변환` ➔ `적응형 히스토그램 평활화 (CLAHE)` ➔ `가우시안 블러 노이즈 제거` ➔ `해상도 최적화(업스케일링/다운스케일링)` ➔ `샤프닝(윤곽선 강화)`\n')
    f.write('- 전자 영수증 등 원본 디지털 파일이 들어올 경우 원본의 선명함을 손상시키지 않고, 흐릿하거나 빛 번짐이 있는 스캔 영수증만 스마트하게 화질을 개선하여 문자를 뚜렷하게 복원합니다.\n\n')
    f.write('### 2) 은행 이체증명서(급여/수수료) 자동 행 분할\n')
    f.write('- 중국 은행 영수증(银行回单) 내에 **급여(工资)** 키워드와 하단의 **수수료(手续费)** 금액이 함께 기재된 경우, 시스템이 이를 인지하여 엑셀 정산 시 **자동으로 2줄(급여 1줄, 수수료 1줄)**로 분할하여 기입하도록 `receipt_parser.py` 및 `batch_processor.py` 로직을 업그레이드했습니다.\n')
    f.write('- 이로써 기존 5월 증빙(61-62번) 뿐만 아니라, 향후 6월 이후 영수증에서도 급여 송금 내역이 업로드되면 자동으로 2줄 분리 입력되어 수기 보정의 번거로움이 사라졌습니다.\n')
